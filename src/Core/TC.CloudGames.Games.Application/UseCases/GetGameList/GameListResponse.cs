namespace TC.CloudGames.Games.Application.UseCases.GetGameList
{
    [SuppressMessage("Minor Code Smell", "S2094:Classes should not be empty", Justification = "Avoid duplicate class, since they are identical")]
    public class GameListResponse : GameByIdResponse
    {
        ////
    }
}
